<div class="m-3">
  <div class="d-flex justify-content-around p-3 bg-success rounded text-white">
    <div class="d-flex flex-column">
      <h1 class=""><%= @item.name %></h1>
      <br>
      <% price_str = number_with_precision( @item.price, :precision => 2).to_s+"€" %>
      <h1 class="d-inline-flex align-self-center"><%= price_str %></h1>
    </div>
    <div class="d-flex flex-column">
      <h3 class="d-inline-flex align-self-center">   in <%= @menu.title %></h3>
      <h3 class="d-inline-flex align-self-center">/<%= @submenu.title %></h3>
    </div>
  </div>

  <hr>
   <%= render "edit_item" %>
</div>